- 简单、低耦合的星露谷功能归入 `packages/Toolbox`；较大型或需要独立生命周期、依赖和配置的功能建立独立包；用户明确指定独立包时优先遵循该边界。
- 每个包拥有独立的 SMAPI `manifest.json`、UniqueID 和构建产物；不要让工具箱与独立包共享运行时身份。
- 工具箱功能改动通过验证后，始终将构建产物安装到游戏的 `Mods/Toolbox`，且不得覆盖现有配置。
